import type { Operator } from "../types";

/** The fictional operator the universe is built around. */
export const OPERATOR: Operator = {
  id: "northwind",
  name: "Northwind Expeditions",
  hq: "Santiago, Chile",
};
