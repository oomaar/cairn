/**
 * Master seed for the simulated universe. The same seed produces the same
 * operational universe on every build — better debugging, screenshots,
 * reproducibility, and consistency. Change it to roll a brand-new (but still
 * deterministic) world.
 */
export const UNIVERSE_SEED = "cairn-northwind-2026";
